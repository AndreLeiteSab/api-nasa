# Base URL of the FastAPI backend. In dev the Vite proxy handles /api,
# so the default empty value works. For production builds point this to
# the deployed backend, e.g. https://meu-backend.com
VITE_API_BASE=
